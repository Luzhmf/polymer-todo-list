class ListItem {
    id: String;
    item: String;
    done: Boolean

    constructor(id: String, item: String, done: Boolean) {
        this.id = id;
        this.item = item;
        this.done = done;
    }
}